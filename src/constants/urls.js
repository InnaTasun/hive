const domain = 'https://jsonplaceholder.typicode.com';

export const URL_USERS = `${domain}/users`;
export const URL_ALBUMS = `${domain}/albums`;
export const URL_PHOTOS = `${domain}/photos`;
export const URL_POSTS = `${domain}/posts`;
